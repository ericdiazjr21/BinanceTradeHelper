version: 2.0
jobs:
  build:
    docker:
      - image: circleci/android:api-28-alpha
    steps:
      - run:
          name: Test Run!
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'

